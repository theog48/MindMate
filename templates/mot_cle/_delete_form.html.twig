<form method="post" action="{{ path('app_mot_cle_delete', {'id': mot_cle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mot_cle.id) }}">
    <button class="btn">Delete</button>
</form>
