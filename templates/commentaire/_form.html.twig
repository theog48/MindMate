<div class="form-wrapper">
	{{ form_start(form) }}
	{{ form_widget(form.titre, { 'attr': {'placeholder': 'Titre de votre cours'} }) }}
	{{ form_widget(form.contenu, { 'attr': {'placeholder': 'Entrez votre cours ici'} }) }}
	<button type="submit">{{ button_label|default('Crée un cours') }}</button>
	{{ form_end(form) }}
</div>
